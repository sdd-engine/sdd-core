{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Project Scaffolding Input",
  "description": "Input parameters for scaffolding a new SDD project structure.",
  "type": "object",
  "properties": {
    "mode": {
      "type": "string",
      "enum": [
        "minimal",
        "full"
      ],
      "description": "Scaffolding mode: minimal creates essential files only, full creates complete structure"
    },
    "project_name": {
      "type": "string",
      "description": "Name of the project to scaffold"
    },
    "target_dir": {
      "type": "string",
      "description": "Absolute path to the target directory for scaffolding"
    },
    "project_description": {
      "type": "string",
      "description": "Brief description of the project"
    },
    "primary_domain": {
      "type": "string",
      "description": "Primary business domain for the project"
    }
  },
  "required": [
    "mode",
    "project_name",
    "target_dir"
  ]
}
